using System;
namespace FirstCourse.Arrays
{
    public class Example
    {
        // Array, non-primitive type
        // Array(Object) has fixed size and it can't be changed

        /*
        // Var
        int number1;
        int number2;
        int number3;

        // or instead we can Declaring Arrays 
        int[] numbers = new int[3];  // 'new' oparator to allocate memory for it

        // Accessing Array Elements
        */

        public static void Main(string[] args)
        {
            /*
            int[] numbers = new int[3];
            numbers[0] = 1;
            numbers[1] = 2;
            numbers[2] = 3;
            */

            int[] numbers = new int[3] { 1, 2, 3 };

        }
    }
}
